<div class="bg-blue-300 p-3" style="width: 100%; height: 150px">
  <div class="flex flex-wrap justify-center p-5">
    <div class="flex">
      <% (0..28).each do %>
          <%= champion_image_tag(champion: champions, classes: "h-9 w-9 mx-0.5 my-0.5") %>
        <% end %>
      </div>
      <div class="flex">
        <% (0..28).each do %>
          <%= champion_image_tag(champion: champions, classes: "h-9 w-9 mx-0.5 my-0.5") %>
      <% end %>
    </div>
  </div>
</div>
